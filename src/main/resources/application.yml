spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.h2.Driver
    #    url: jdbc:h2:tcp://localhost/~/test
    url: jdbc:h2:tcp://localhost/~/h2/spring
    username: sa
    password:
